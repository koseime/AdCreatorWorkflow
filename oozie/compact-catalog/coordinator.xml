<coordinator-app name="compact-catalog-coord" frequency="1440" start="${startCoordDate}" end="${endCoordDate}" timezone="GMT" xmlns="uri:oozie:coordinator:0.1">
    <action>
        <workflow>
            <app-path>${workflowRoot}/workflow.xml</app-path>
        </workflow>
    </action>
</coordinator-app>
